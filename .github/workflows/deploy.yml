name: deploy

# STUB — intentionally disabled until Phase 7.
# Manual-trigger only (workflow_dispatch), so it never runs on push/PR. When the
# app is ready to ship, this becomes the static-site deploy (Cloudflare Pages or
# GitHub Pages): build web/, then publish web/dist. See PROJECTPLAN.md Phase 7.

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "Deploy is not configured yet (Phase 7). See PROJECTPLAN.md."
          echo "This stub exists so the workflow file is in place and greppable."
